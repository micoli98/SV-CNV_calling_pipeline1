# Don't track files in the post_processing since they belong to a separate pipeline
post_processing/
other_scripts/
old_version/

# Remove also nextflow.config from tracking
nextflow.config

# Remove logs
*.log*
.nextflow/

# Remove trials and other pipelines
tumor_only/
trials/

# Remove working directory
work/
